import React from 'react';

const Header = () => {
  return (
    <header className="App-header">
      <h1>Movie Search App</h1>
      {/* You can add navigation or other header elements here later */}
    </header>
  );
};

export default Header;